<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider paths: app-owned capture files shared with the system
     camera app (ACTION_IMAGE_CAPTURE). Nothing outside the cache dir is
     exposed. -->
<paths>
    <cache-path name="captures" path="captures/"/>
</paths>
